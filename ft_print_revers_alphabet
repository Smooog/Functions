#include <unistd.h>

void ft_print_revers_alphabet(void){
    int a;

    a = 122;
    while(a != 96){
        write (1, &a, 1);
        a--;
    }
}

int main(){
    ft_print_revers_alphabet();
    return 0;
}
